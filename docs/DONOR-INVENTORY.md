# Donor Inventory

## Pinned donors

- `StealthEyeLLC/baby-quirt` commit `af130f2ea17ccc5a8f085b0c5db016ede3eee02f`.
- `StealthEyeLLC/baby-quirt-mcp` commit `9d224744369bc5581584cfa043058ecb55580025`.

The runtime donor was materialized at the exact pin for implementation inspection. The private gateway donor requires authenticated materialization and must be independently verified before claiming exact extraction completion.

## Preserved concepts

QRT1 framing, Unix socket activation, peer credentials, Ed25519 canonical signatures, replay rejection, durable jobs and streams, process identity, binary-safe files, PTYs, artifacts, OAuth gateway separation, and dynamic operation discovery.

## Rewritten concepts

Operation definitions, root authority, compact proof, host/machine targeting, systemd authority, nspawn machine fabric, provider discovery, specifications, campaigns, and counterexamples.

## Excluded architecture

Deployment state machines, release transition databases, confirmation digests, risk tiers, heavy receipts, evidence indexes, fixed-plan execution, guarded activation ceremony, repository-scoped safe publication, and GitHub Actions workflows.

No production state, credentials, private keys, installed manifests, or runtime databases are donor source material.
